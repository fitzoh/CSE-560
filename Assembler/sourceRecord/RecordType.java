package sourceRecord;

/**
 * Enum class for the types of records that need treated differently.
 * @author Dragon Slayer
 */
public enum RecordType {
	ORI, END, RES, COMMENT, EQU, OTHER, LITERAL, ENT, EXT, EXEC //Machine ops are represented by EXEC
}
